<!-- 비밀번호 재설정 요청 페이지 -->

<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>비밀번호 재설정</title>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <style>
        body {
            margin: 0;
            padding: 0;
            background: #f5f5f5;
            color: #000;
            font-family: 'Arial', sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            flex-direction: column;
        }

        .title-container {
            position: absolute;
            top: 20px;
            width: 100%;
            display: flex;
            justify-content: center;
        }

        .title {
            font-size: 4rem;
            color: #0055ff;
            background: rgba(255, 255, 255, 0.9);
            padding: 20px 40px;
            border-radius: 10px;
            box-shadow: 0 0 15px 2px #0055ff;
            text-align: center;
        }

        .reset-container {
            background: rgba(255, 255, 255, 0.9);
            padding: 2rem;
            border-radius: 10px;
            box-shadow: 0 0 15px 2px #0055ff;
            text-align: center;
            width: 100%;
            max-width: 400px;
        }

        h1 {
            font-size: 2rem;
            color: #0055ff;
            margin-bottom: 1.5rem;
        }

        p {
            margin-bottom: 1.5rem;
            line-height: 1.5;
        }

        form {
            display: flex;
            flex-direction: column;
            gap: 1rem;
        }

        .input-group {
            display: flex;
            flex-direction: column;
            text-align: left;
        }

        .input-group label {
            margin-bottom: 0.3rem;
            font-weight: bold;
            font-size: 0.9rem;
        }

        input {
            padding: 0.8rem;
            border: 1px solid #ddd;
            border-radius: 5px;
            font-size: 1rem;
            background: #f0f0f0;
            color: #000;
            transition: border-color 0.3s;
        }

        input:focus {
            outline: none;
            border-color: #0055ff;
            box-shadow: 0 0 5px rgba(0, 85, 255, 0.3);
        }

        input.error-field {
            border-color: #ff5555;
            background-color: rgba(255, 85, 85, 0.05);
        }

        button {
            padding: 0.8rem;
            border: none;
            border-radius: 5px;
            font-size: 1rem;
            font-weight: bold;
            background: #0055ff;
            color: #fff;
            cursor: pointer;
            transition: transform 0.2s, box-shadow 0.2s, background-color 0.2s;
            margin-top: 1rem;
        }

        button:hover:not(:disabled) {
            transform: scale(1.05);
            box-shadow: 0 0 10px #4477ff;
        }

        button:disabled {
            background-color: #999;
            cursor: not-allowed;
        }

        a {
            color: #0055ff;
            text-decoration: none;
            display: inline-block;
            margin-top: 1rem;
        }

        a:hover {
            text-decoration: underline;
        }

        .success-message {
            margin-top: 1rem;
            color: #55aa55;
            font-size: 0.9rem;
            background: rgba(85, 170, 85, 0.1);
            padding: 0.5rem;
            border-radius: 5px;
            display: none;
        }

        .error-message {
            margin-top: 1rem;
            color: #ff5555;
            font-size: 0.9rem;
            background: rgba(255, 85, 85, 0.1);
            padding: 0.5rem;
            border-radius: 5px;
            display: none;
        }
    </style>
</head>
<body>
    <div class="title-container">
        <div class="title">머드게임</div>
    </div>
    <div class="reset-container">
        <h1>비밀번호 재설정</h1>
        
        <div id="reset-form">
            <p>계정에 등록된 이메일 주소를 입력하시면 비밀번호 재설정 링크를 발송해 드립니다.</p>
            
            <form id="resetRequestForm">
                <!-- CSRF 토큰 추가 -->
                <input type="hidden" name="_csrf" id="csrfToken" value="<%= csrfToken %>">
                
                <div class="input-group">
                    <label for="email">이메일</label>
                    <input type="email" id="email" name="email" placeholder="계정에 등록된 이메일 주소" required>
                </div>
                
                <button type="submit" id="submitButton">재설정 링크 발송</button>
            </form>
            
            <a href="/auth/login">로그인 페이지로 돌아가기</a>
        </div>
        
        <div id="successMessage" class="success-message"></div>
        <div id="errorMessage" class="error-message"></div>
    </div>

    <script>
        $(document).ready(function() {
            $('#resetRequestForm').on('submit', function(e) {
                e.preventDefault();
                
                // 메시지 초기화
                $('#successMessage, #errorMessage').hide().text('');
                
                // 버튼 비활성화
                $('#submitButton').prop('disabled', true).text('처리 중...');
                
                $.ajax({
                    url: '/auth/reset-password/request',
                    method: 'POST',
                    contentType: 'application/json',
                    headers: {
                        'CSRF-Token': $('#csrfToken').val()
                    },
                    data: JSON.stringify({
                        email: $('#email').val()
                    }),
                    success: function(response) {
                        $('#reset-form').hide();
                        $('#successMessage').text(response.msg).show();
                    },
                    error: function(xhr) {
                        const response = xhr.responseJSON || {};
                        $('#errorMessage').text(response.msg || '비밀번호 재설정 요청 중 오류가 발생했습니다.').show();
                        $('#submitButton').prop('disabled', false).text('재설정 링크 발송');
                    }
                });
            });
        });
    </script>
</body>
</html>