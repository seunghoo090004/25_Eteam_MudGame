<!-- views/endings-list.ejs - 엔딩 목록 페이지 -->

<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>엔딩 기록 - 던전 탈출</title>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <style>
        body {
            margin: 0;
            padding: 0;
            background: linear-gradient(135deg, #1a1a2e 0%, #16213e 50%, #0f3460 100%);
            color: #ffffff;
            font-family: 'Arial', sans-serif;
            min-height: 100vh;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 2rem;
        }

        .header {
            text-align: center;
            margin-bottom: 3rem;
            background: rgba(0, 0, 0, 0.6);
            padding: 2rem;
            border-radius: 15px;
            border: 2px solid #007bff;
        }

        .header h1 {
            font-size: 2.5rem;
            margin: 0;
            color: #00bcd4;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.8);
        }

        .header p {
            font-size: 1.2rem;
            margin: 1rem 0 0 0;
            opacity: 0.9;
        }

        .stats-summary {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 1rem;
            margin-bottom: 3rem;
        }

        .stat-card {
            background: rgba(40, 40, 40, 0.8);
            border: 1px solid #666;
            border-radius: 10px;
            padding: 1.5rem;
            text-align: center;
            transition: transform 0.3s ease;
        }

        .stat-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(0, 123, 255, 0.3);
        }

        .stat-number {
            font-size: 2.5rem;
            font-weight: bold;
            color: #00bcd4;
            display: block;
        }

        .stat-label {
            font-size: 1rem;
            color: #ccc;
            margin-top: 0.5rem;
        }

        .endings-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
            gap: 2rem;
            margin-bottom: 3rem;
        }

        .ending-card {
            background: rgba(30, 30, 30, 0.9);
            border: 2px solid #444;
            border-radius: 12px;
            padding: 1.5rem;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        .ending-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.5);
        }

        .ending-card.death {
            border-color: #dc3545;
            background: linear-gradient(135deg, rgba(220, 53, 69, 0.1), rgba(30, 30, 30, 0.9));
        }

        .ending-card.escape {
            border-color: #28a745;
            background: linear-gradient(135deg, rgba(40, 167, 69, 0.1), rgba(30, 30, 30, 0.9));
        }

        .ending-card.quit {
            border-color: #6c757d;
            background: linear-gradient(135deg, rgba(108, 117, 125, 0.1), rgba(30, 30, 30, 0.9));
        }

        .ending-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 1rem;
            padding-bottom: 0.5rem;
            border-bottom: 1px solid #555;
        }

        .ending-type {
            font-size: 1.2rem;
            font-weight: bold;
            padding: 0.3rem 0.8rem;
            border-radius: 20px;
        }

        .ending-type.death {
            background: #dc3545;
            color: white;
        }

        .ending-type.escape {
            background: #28a745;
            color: white;
        }

        .ending-type.quit {
            background: #6c757d;
            color: white;
        }

        .ending-date {
            font-size: 0.9rem;
            color: #aaa;
        }

        .ending-stats {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 1rem;
            margin: 1rem 0;
        }

        .ending-stat {
            text-align: center;
            padding: 0.8rem;
            background: rgba(0, 0, 0, 0.3);
            border-radius: 8px;
        }

        .ending-stat-value {
            font-size: 1.5rem;
            font-weight: bold;
            color: #00bcd4;
        }

        .ending-stat-label {
            font-size: 0.8rem;
            color: #ccc;
            margin-top: 0.3rem;
        }

        .ending-story {
            margin-top: 1rem;
            padding: 1rem;
            background: rgba(0, 0, 0, 0.2);
            border-left: 3px solid #007bff;
            border-radius: 0 8px 8px 0;
            font-style: italic;
            line-height: 1.5;
            max-height: 100px;
            overflow: hidden;
            position: relative;
        }

        .ending-story.expanded {
            max-height: none;
        }

        .expand-btn {
            background: none;
            border: none;
            color: #007bff;
            cursor: pointer;
            font-size: 0.9rem;
            margin-top: 0.5rem;
            text-decoration: underline;
        }

        .cause-of-death {
            margin-top: 1rem;
            padding: 0.8rem;
            background: rgba(220, 53, 69, 0.2);
            border: 1px solid rgba(220, 53, 69, 0.5);
            border-radius: 8px;
            font-size: 0.9rem;
        }

        .no-endings {
            text-align: center;
            padding: 4rem 2rem;
            background: rgba(40, 40, 40, 0.8);
            border-radius: 15px;
            margin: 2rem 0;
        }

        .no-endings h3 {
            color: #00bcd4;
            font-size: 1.8rem;
            margin-bottom: 1rem;
        }

        .back-button {
            position: fixed;
            top: 2rem;
            left: 2rem;
            background: linear-gradient(135deg, #007bff, #0056b3);
            color: white;
            border: none;
            border-radius: 50px;
            padding: 1rem 1.5rem;
            font-size: 1rem;
            font-weight: bold;
            cursor: pointer;
            transition: all 0.3s ease;
            text-decoration: none;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            z-index: 1000;
        }

        .back-button:hover {
            background: linear-gradient(135deg, #0056b3, #003f7f);
            transform: translateY(-2px);
            box-shadow: 0 10px 20px rgba(0, 123, 255, 0.4);
        }

        @media (max-width: 768px) {
            .container {
                padding: 1rem;
            }
            
            .header h1 {
                font-size: 2rem;
            }
            
            .endings-grid {
                grid-template-columns: 1fr;
            }
            
            .stats-summary {
                grid-template-columns: repeat(2, 1fr);
            }
            
            .back-button {
                position: static;
                margin-bottom: 2rem;
                align-self: flex-start;
            }
        }
    </style>
</head>
<body>
    <a href="/" class="back-button">
        ← 메인으로 돌아가기
    </a>

    <div class="container">
        <div class="header">
            <h1>📚 엔딩 기록</h1>
            <p><%= username %>님의 던전 탈출 기록</p>
        </div>

        <% if (endings && endings.length > 0) { %>
            <!-- 통계 요약 -->
            <div class="stats-summary">
                <div class="stat-card">
                    <span class="stat-number"><%= endings.length %></span>
                    <div class="stat-label">총 게임 수</div>
                </div>
                <div class="stat-card">
                    <span class="stat-number"><%= endings.filter(e => e.ending_type === 'death').length %></span>
                    <div class="stat-label">사망 횟수</div>
                </div>
                <div class="stat-card">
                    <span class="stat-number"><%= endings.filter(e => e.ending_type === 'escape').length %></span>
                    <div class="stat-label">탈출 성공</div>
                </div>
                <div class="stat-card">
                    <span class="stat-number"><%= Math.round(endings.reduce((sum, e) => sum + (e.final_turn || 0), 0) / endings.length) %></span>
                    <div class="stat-label">평균 생존 턴</div>
                </div>
            </div>

            <!-- 엔딩 목록 -->
            <div class="endings-grid">
                <% endings.forEach(function(ending) { %>
                <div class="ending-card <%= ending.ending_type %>">
                    <div class="ending-header">
                        <span class="ending-type <%= ending.ending_type %>">
                            <% if (ending.ending_type === 'death') { %>
                                💀 사망
                            <% } else if (ending.ending_type === 'escape') { %>
                                ✨ 탈출 성공
                            <% } else { %>
                                🚪 게임 종료
                            <% } %>
                        </span>
                        <span class="ending-date">
                            <%= new Date(ending.completed_at).toLocaleDateString('ko-KR') %>
                        </span>
                    </div>

                    <div class="ending-stats">
                        <div class="ending-stat">
                            <div class="ending-stat-value"><%= ending.final_turn || 1 %></div>
                            <div class="ending-stat-label">최종 턴</div>
                        </div>
                        <div class="ending-stat">
                            <div class="ending-stat-value"><%= ending.total_deaths || 0 %></div>
                            <div class="ending-stat-label">총 사망</div>
                        </div>
                        <div class="ending-stat">
                            <div class="ending-stat-value"><%= ending.discoveries_count || 0 %></div>
                            <div class="ending-stat-label">발견 정보</div>
                        </div>
                        <div class="ending-stat">
                            <div class="ending-stat-value"><%= ending.time_elapsed || 0 %></div>
                            <div class="ending-stat-label">플레이 시간(분)</div>
                        </div>
                    </div>

                    <% if (ending.ending_type === 'death' && ending.cause_of_death) { %>
                    <div class="cause-of-death">
                        <strong>💀 사망 원인:</strong> <%= ending.cause_of_death %>
                    </div>
                    <% } %>

                    <% if (ending.ending_story) { %>
                    <div class="ending-story" data-full-story="<%= ending.ending_story.replace(/"/g, '&quot;') %>">
                        <%= ending.ending_story.length > 100 ? ending.ending_story.substring(0, 100) + '...' : ending.ending_story %>
                        <% if (ending.ending_story.length > 100) { %>
                        <button class="expand-btn" onclick="toggleStory(this)">더 보기</button>
                        <% } %>
                    </div>
                    <% } %>
                </div>
                <% }); %>
            </div>
        <% } else { %>
            <div class="no-endings">
                <h3>🎮 아직 완료된 게임이 없습니다</h3>
                <p>던전 탈출에 도전해보세요!</p>
                <a href="/" class="back-button" style="position: static; margin-top: 2rem;">새 게임 시작하기</a>
            </div>
        <% } %>
    </div>

    <script>
        function toggleStory(button) {
            const storyDiv = button.parentElement;
            const fullStory = storyDiv.getAttribute('data-full-story');
            
            if (storyDiv.classList.contains('expanded')) {
                // 축소
                storyDiv.innerHTML = fullStory.substring(0, 100) + '... <button class="expand-btn" onclick="toggleStory(this)">더 보기</button>';
                storyDiv.classList.remove('expanded');
            } else {
                // 확장
                storyDiv.innerHTML = fullStory + ' <button class="expand-btn" onclick="toggleStory(this)">접기</button>';
                storyDiv.classList.add('expanded');
            }
        }

        // 키보드 단축키
        $(document).keydown(function(e) {
            if (e.key === 'Escape' || e.key === 'Backspace') {
                window.location.href = '/';
            }
        });
    </script>
</body>
</html>