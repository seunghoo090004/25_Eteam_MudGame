<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - 던전 탈출</title>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <style>
        body {
            margin: 0;
            padding: 0;
            background: linear-gradient(135deg, #1a1a2e 0%, #16213e 50%, #0f3460 100%);
            color: white;
            font-family: 'Arial', sans-serif;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .ending-container {
            max-width: 900px;
            width: 90%;
            background: rgba(0, 0, 0, 0.8);
            border-radius: 20px;
            padding: 40px;
            box-shadow: 0 0 30px rgba(0, 123, 255, 0.3);
            text-align: center;
            border: 2px solid rgba(0, 123, 255, 0.5);
        }

        .ending-title {
            font-size: 3rem;
            margin-bottom: 20px;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.8);
        }

        .ending-title.death {
            color: #ff4757;
            text-shadow: 0 0 20px #ff4757;
        }

        .ending-title.escape {
            color: #2ed573;
            text-shadow: 0 0 20px #2ed573;
        }

        .ending-title.special {
            color: #ffa502;
            text-shadow: 0 0 20px #ffa502;
        }

        .ending-story {
            font-size: 1.2rem;
            line-height: 1.8;
            margin: 30px 0;
            padding: 20px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 10px;
            border-left: 4px solid #0055ff;
        }

        .stats-container {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin: 30px 0;
        }

        .stat-box {
            background: rgba(255, 255, 255, 0.1);
            padding: 20px;
            border-radius: 10px;
            border: 1px solid rgba(255, 255, 255, 0.2);
        }

        .stat-label {
            font-size: 0.9rem;
            color: #aaa;
            margin-bottom: 5px;
        }

        .stat-value {
            font-size: 2rem;
            font-weight: bold;
            color: #0055ff;
        }

        .stat-value.death-count {
            color: #ff4757;
        }

        .stat-value.turn-count {
            color: #2ed573;
        }

        .achievement-badge {
            display: inline-block;
            background: linear-gradient(45deg, #ffa502, #ff6b35);
            padding: 10px 20px;
            border-radius: 20px;
            margin: 10px;
            font-weight: bold;
            box-shadow: 0 4px 15px rgba(255, 165, 2, 0.3);
        }

        .buttons-container {
            margin-top: 40px;
        }

        .btn {
            padding: 15px 30px;
            margin: 10px;
            border: none;
            border-radius: 10px;
            font-size: 1.1rem;
            font-weight: bold;
            cursor: pointer;
            transition: all 0.3s ease;
            text-decoration: none;
            display: inline-block;
        }

        .btn-primary {
            background: linear-gradient(45deg, #0055ff, #0066cc);
            color: white;
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(0, 85, 255, 0.4);
        }

        .btn-secondary {
            background: linear-gradient(45deg, #666, #888);
            color: white;
        }

        .btn-secondary:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(136, 136, 136, 0.4);
        }

        .death-details {
            margin: 20px 0;
            padding: 15px;
            background: rgba(255, 71, 87, 0.1);
            border-radius: 10px;
            border-left: 4px solid #ff4757;
        }

        .escape-details {
            margin: 20px 0;
            padding: 15px;
            background: rgba(46, 213, 115, 0.1);
            border-radius: 10px;
            border-left: 4px solid #2ed573;
        }

        .special-details {
            margin: 20px 0;
            padding: 15px;
            background: rgba(255, 165, 2, 0.1);
            border-radius: 10px;
            border-left: 4px solid #ffa502;
        }

        .floating-particles {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: -1;
        }

        .particle {
            position: absolute;
            width: 4px;
            height: 4px;
            background: rgba(0, 123, 255, 0.6);
            border-radius: 50%;
            animation: float 6s infinite linear;
        }

        @keyframes float {
            0% {
                transform: translateY(100vh) rotate(0deg);
                opacity: 0;
            }
            10% {
                opacity: 1;
            }
            90% {
                opacity: 1;
            }
            100% {
                transform: translateY(-100vh) rotate(360deg);
                opacity: 0;
            }
        }

        .congratulations {
            font-size: 1.5rem;
            color: #2ed573;
            margin: 20px 0;
            text-shadow: 0 0 10px #2ed573;
        }

        .game-over {
            font-size: 1.5rem;
            color: #ff4757;
            margin: 20px 0;
            text-shadow: 0 0 10px #ff4757;
        }
    </style>
</head>
<body>
    <!-- 배경 파티클 효과 -->
    <div class="floating-particles">
        <% for(let i = 0; i < 20; i++) { %>
            <div class="particle" style="left: <%= Math.random() * 100 %>%; animation-delay: <%= Math.random() * 6 %>s;"></div>
        <% } %>
    </div>

    <div class="ending-container">
        <!-- 엔딩 제목 -->
        <h1 class="ending-title <%= type %>"><%= title %></h1>

        <!-- 타입별 메시지 -->
        <% if (type === 'escape') { %>
            <div class="congratulations">🎉 축하합니다! 불가능에 가까운 탈출을 해내셨습니다! 🎉</div>
        <% } else if (type === 'death') { %>
            <div class="game-over">💀 던전이 또 다른 영혼을 집어삼켰습니다... 💀</div>
        <% } else if (type === 'special') { %>
            <div class="congratulations">⭐ 전설적인 업적을 달성하셨습니다! ⭐</div>
        <% } %>

        <!-- 엔딩 스토리 -->
        <div class="ending-story">
            <%= story %>
        </div>

        <!-- 통계 정보 -->
        <div class="stats-container">
            <div class="stat-box">
                <div class="stat-label">생존한 턴 수</div>
                <div class="stat-value turn-count"><%= turnCount %></div>
            </div>
            <div class="stat-box">
                <div class="stat-label">총 사망 횟수</div>
                <div class="stat-value death-count"><%= deathCount %></div>
            </div>
            <% if (type === 'escape') { %>
                <div class="stat-box">
                    <div class="stat-label">탈출 성공률</div>
                    <div class="stat-value"><%= Math.round((1 / (deathCount + 1)) * 100) %>%</div>
                </div>
            <% } %>
            <% if (turnCount >= 20) { %>
                <div class="stat-box">
                    <div class="stat-label">생존 등급</div>
                    <div class="stat-value">마스터</div>
                </div>
            <% } else if (turnCount >= 15) { %>
                <div class="stat-box">
                    <div class="stat-label">생존 등급</div>
                    <div class="stat-value">숙련자</div>
                </div>
            <% } else if (turnCount >= 10) { %>
                <div class="stat-box">
                    <div class="stat-label">생존 등급</div>
                    <div class="stat-value">초보자</div>
                </div>
            <% } %>
        </div>

        <!-- 타입별 세부 정보 -->
        <% if (type === 'death') { %>
            <div class="death-details">
                <strong>사망 원인:</strong> <%= cause %><br>
                <% if (turnCount <= 5) { %>
                    던전의 초기 함정들도 극복하지 못했습니다. 더 신중한 접근이 필요합니다.
                <% } else if (turnCount <= 10) { %>
                    초기 단계는 넘겼지만 본격적인 위험에서 무너졌습니다. 조금만 더 버텼다면...
                <% } else { %>
                    탈출 모드까지 도달했지만 마지막 관문을 넘지 못했습니다. 정말 아쉽습니다!
                <% } %>
            </div>
        <% } else if (type === 'escape') { %>
            <div class="escape-details">
                <strong>탈출 방법:</strong> <%= method %><br>
                <% if (turnCount <= 15) { %>
                    효율적인 탈출! 최소한의 시행착오로 목표를 달성했습니다.
                <% } else if (turnCount <= 25) { %>
                    끈질긴 탈출! 포기하지 않는 의지가 승리를 가져다주었습니다.
                <% } else { %>
                    장대한 탈출! 길고 험난한 여정 끝에 얻은 값진 승리입니다.
                <% } %>
            </div>
        <% } else if (type === 'special') { %>
            <div class="special-details">
                <div class="achievement-badge"><%= achievement %></div>
                <br>이는 던전 역사상 극소수만이 달성한 위업입니다!
            </div>
        <% } %>

        <!-- 버튼들 -->
        <div class="buttons-container">
            <a href="/" class="btn btn-primary">새 게임 시작</a>
            <a href="/auth/logout" class="btn btn-secondary">로그아웃</a>
        </div>

        <!-- 플레이어 정보 -->
        <div style="margin-top: 30px; color: #aaa; font-size: 0.9rem;">
            플레이어: <%= playerName %> | 
            플레이 완료 시간: <%= new Date().toLocaleString() %>
        </div>
    </div>

    <script>
        $(document).ready(function() {
            var endingType = '<%- type %>';
            
            $('.ending-container').hide().fadeIn(1000);
            
            // 통계 카운트업 애니메이션
            $('.stat-value').each(function() {
                const $this = $(this);
                const targetValue = parseInt($this.text());
                
                if (!isNaN(targetValue)) {
                    $this.text('0');
                    $({ counter: 0 }).animate({ counter: targetValue }, {
                        duration: 2000,
                        step: function() {
                            $this.text(Math.ceil(this.counter));
                        }
                    });
                }
            });
            
            // 축하 메시지 깜빡임 효과 (탈출 성공 시)
            if (endingType === 'escape' || endingType === 'special') {
                setInterval(function() {
                    $('.congratulations').fadeOut(500).fadeIn(500);
                }, 2000);
            }
        });
    </script>
</body>
</html>